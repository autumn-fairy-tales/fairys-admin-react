# 主菜单

:::tip layout内置组件

项目主菜单组件，用于显示项目的主菜单。

:::

**引入**

```ts
import { MainMenu } from '@fairys/admin-tools-react';
```

## 参数

```ts
/**
 *  主菜单分为 左侧主菜单 和 顶部主菜单
 */
export interface MainMenuProps {
    layoutMode?: 'vertical' | 'horizontal';
}

```